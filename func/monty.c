#include<stdint.h>

#define bignum(a) (Word a[_N_INTERNAL_LEN])

void bgcd(Word r[], Word m[], Word nr[], Word nm[]){
    bignum(u);
    bignum(v);
    bignum(alph);
    bignum(beta);
    load(u, 0);
    load(v, 1);
    copy(r, alph);
    copy(m, beta);

    while(!(zerop(a))){
        half(a);
        if(evenp(u)){
            half(u);
            half(v);
        }
        else{
            c = add(u, beta, 
        }
    }
}
